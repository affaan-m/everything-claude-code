{
  "scripts": {
    "lint": "npx eslint 'scripts/**/*.js' 'tests/**/*.js' && npx markdownlint 'agents/**/*.md' 'skills/**/*.md' 'commands/**/*.md' 'rules/**/*.md'",
    "build": "echo 'Build step complete - no compilation required'"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^10.0.1",
    "globals": "^17.1.0",
    "markdownlint-cli": "^0.12.0"
  }
}
