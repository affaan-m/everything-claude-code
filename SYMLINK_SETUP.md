# シンボリックリンクセットアップガイド

このリポジトリのファイルを`~/.claude`からシンボリックリンクで参照できるようにします。

## 🎯 目的

- ✅ リポジトリで編集 → Claude Codeが即座に反映
- ✅ Git管理された状態で開発
- ✅ プッシュして他の環境と同期
- ✅ バックアップ不要（Gitで管理）

## 🚀 クイックスタート

```bash
# リポジトリのディレクトリに移動
cd /home/shaya/everything-claude-code

# シンボリックリンクをセットアップ
./scripts/setup-symlinks.sh
```

## 📂 リンクされるディレクトリ

以下のディレクトリが`~/.claude/`からシンボリックリンクされます：

### 必須ディレクトリ（自動リンク）

| ディレクトリ | 説明 | リンク先 |
|------------|------|---------|
| `agents/` | サブエージェント定義 | `~/.claude/agents/` |
| `commands/` | スラッシュコマンド | `~/.claude/commands/` |
| `skills/` | ワークフロー定義 | `~/.claude/skills/` |
| `scripts/` | ヘルパースクリプト | `~/.claude/scripts/` |
| `rules/` | グローバルルール | `~/.claude/rules/` |
| `hooks/` | フック定義 | `~/.claude/hooks/` |

### オプションディレクトリ（手動リンク）

必要に応じて手動でリンク可能：

```bash
# MCP設定をリンク
ln -s ~/everything-claude-code/mcp-configs ~/.claude/mcp-configs

# コンテキスト設定をリンク
ln -s ~/everything-claude-code/contexts ~/.claude/contexts
```

## 🔒 安全機能

### 自動バックアップ

既存のディレクトリは自動的にバックアップされます：

```
~/.claude/backup-20260124-214000/
├── agents/
├── commands/
├── skills/
└── ...
```

### ロールバック方法

元に戻したい場合：

```bash
# シンボリックリンクを削除
rm -rf ~/.claude/{agents,commands,skills,scripts,rules,hooks}

# バックアップから復元
mv ~/.claude/backup-YYYYMMDD-HHMMSS/* ~/.claude/

# バックアップディレクトリを削除
rmdir ~/.claude/backup-YYYYMMDD-HHMMSS
```

## ✅ 動作確認

セットアップ後、以下で確認：

```bash
# シンボリックリンクの確認
ls -la ~/.claude/agents
ls -la ~/.claude/commands

# リンク先の確認
readlink ~/.claude/agents
# 出力: /home/shaya/everything-claude-code/agents
```

## 📝 使い方

### 1. ファイルの編集

リポジトリ内でファイルを編集：

```bash
# リポジトリで編集
vim ~/everything-claude-code/commands/night-shift.md

# Claude Codeが即座に反映
# ~/.claude/commands/night-shift.md は同じファイルを参照
```

### 2. 変更をコミット

```bash
cd ~/everything-claude-code

git add commands/night-shift.md
git commit -m "update: improve night-shift command"
git push origin main
```

### 3. 他の環境で同期

```bash
# 他のマシンで
cd ~/everything-claude-code
git pull origin main

# シンボリックリンクのため、即座に反映
```

## 🔧 トラブルシューティング

### 問題: シンボリックリンクが壊れている

```bash
# 確認
ls -la ~/.claude/agents

# 出力例: agents -> /old/path/agents (壊れたリンク)
```

**解決:**

```bash
# 壊れたリンクを削除
rm ~/.claude/agents

# 正しいリンクを作成
ln -s ~/everything-claude-code/agents ~/.claude/agents
```

### 問題: Permission denied

```bash
# 実行権限を確認
ls -la scripts/setup-symlinks.sh

# 実行権限を追加
chmod +x scripts/setup-symlinks.sh
```

### 問題: ディレクトリが既に存在

スクリプトは自動的にバックアップを作成しますが、手動で対処する場合：

```bash
# 既存のディレクトリを移動
mv ~/.claude/agents ~/.claude/agents.backup

# シンボリックリンクを作成
ln -s ~/everything-claude-code/agents ~/.claude/agents
```

## 📚 リファレンス

### セットアップスクリプトの処理内容

1. **バックアップ作成** - タイムスタンプ付きバックアップディレクトリ作成
2. **既存ディレクトリ移動** - シンボリックリンクでないディレクトリをバックアップへ
3. **古いリンク削除** - 既存のシンボリックリンクを削除
4. **新規リンク作成** - リポジトリへのシンボリックリンク作成
5. **検証** - リンクが正しく作成されたか確認

### ディレクトリ構造

```
~/.claude/
├── agents/ -> ~/everything-claude-code/agents/
├── commands/ -> ~/everything-claude-code/commands/
├── skills/ -> ~/everything-claude-code/skills/
├── scripts/ -> ~/everything-claude-code/scripts/
├── rules/ -> ~/everything-claude-code/rules/
├── hooks/ -> ~/everything-claude-code/hooks/
├── backup-20260124-214000/  # 自動バックアップ
│   ├── agents/
│   └── ...
└── ... (その他の~/.claudeファイル)
```

## 🎓 ベストプラクティス

### 1. 編集前にブランチを作成

```bash
cd ~/everything-claude-code
git checkout -b feature/improve-agents
# 編集...
git commit -m "improve: enhance planner agent"
git push origin feature/improve-agents
```

### 2. 定期的にプッシュ

```bash
# 1日の終わりに
cd ~/everything-claude-code
git add .
git commit -m "daily: update commands and skills"
git push origin main
```

### 3. 他の環境と同期

```bash
# 朝、作業開始時に
cd ~/everything-claude-code
git pull origin main
# シンボリックリンクのため自動的に反映
```

## 🆘 サポート

問題が発生した場合：

1. **バックアップから復元**（上記のロールバック方法参照）
2. **スクリプトを再実行**：`./scripts/setup-symlinks.sh`
3. **手動でリンク作成**（オプションディレクトリの例参照）

---

**Happy Coding! 🚀**
